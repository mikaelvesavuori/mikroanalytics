{
  "name": "mikroanalytics",
  "version": "1.0.0",
  "description": "Private, aggregate-first web and product analytics.",
  "type": "module",
  "license": "MIT",
  "author": "Mikael Vesavuori",
  "keywords": [
    "analytics",
    "web-analytics",
    "product-analytics",
    "privacy",
    "gdpr",
    "self-hosted",
    "sqlite"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikaelvesavuori/mikroanalytics.git"
  },
  "bugs": {
    "url": "https://github.com/mikaelvesavuori/mikroanalytics/issues"
  },
  "homepage": "https://github.com/mikaelvesavuori/mikroanalytics#readme",
  "scripts": {
    "start": "npm run build && node dist/server/server.mjs",
    "start:dist": "node dist/server/server.mjs",
    "demo:seed": "npm run build && node dist/server/demo.mjs",
    "demo:seed:dist": "node dist/server/demo.mjs",
    "deploy": "npm run deploy:frontend && npm run deploy:backend",
    "docs": "npm run docs:install && npm --prefix docs run dev",
    "docs:build": "npm run docs:install && npm --prefix docs run build",
    "docs:preview": "npm run docs:install && npm --prefix docs run preview",
    "clean": "node -e \"const fs=require('node:fs');for (const path of ['dist','coverage']) fs.rmSync(path,{recursive:true,force:true})\"",
    "assets": "node ../scripts/generate-product-icons.mjs --product mikroanalytics",
    "build": "node esbuild.config.mjs",
    "test": "vitest run --config vitest.config.mts --coverage",
    "test:watch": "vitest --config vitest.config.mts",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src tests scripts esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json mikroanalytics.config.json.example",
    "lint:fix": "biome check src tests scripts esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json mikroanalytics.config.json.example --write",
    "format": "biome format --write src tests scripts esbuild.config.mjs vitest.config.mts package.json tsconfig.json biome.json mikroanalytics.config.json.example",
    "release": "npm run build && npm run sbom && npm run scan && npm run licenses && npm run release:assets",
    "verify": "npm run lint && npm run typecheck && npm run test && npm run build && npm run docs:build",
    "deploy:backend": "bash .deployment/deploy-backend.sh",
    "deploy:frontend": "bash .deployment/deploy-frontend.sh",
    "release:assets": "node scripts/create-release-assets.mjs",
    "sbom": "node scripts/create-prod-sbom.mjs",
    "scan": "mkdir -p security && grype sbom:security/sbom.spdx.json -o json > security/grype-report.json",
    "licenses": "mkdir -p security && grant check security/sbom.spdx.json --config .grant.yaml > security/grant-report.txt || { cat security/grant-report.txt; exit 1; }",
    "precommit": "npm run lint && npm run typecheck && npm run test",
    "prepare": "husky",
    "docs:install": "npm --prefix docs ci"
  },
  "engines": {
    "node": ">=25.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@types/node": "25",
    "@vitest/coverage-v8": "4",
    "esbuild": "latest",
    "html-minifier-terser": "7",
    "lightningcss": "1",
    "typescript": "6",
    "vitest": "4",
    "husky": "9.1.7"
  },
  "dependencies": {
    "mikroauth": "1",
    "mikroconf": "1"
  },
  "workspaces": [
    "docs"
  ]
}
